Summary
All box--home behaviors are now fully implemented for dynamic content, matching build.min.js:

Feature	Status
Cursor player - visible on hover, positioned above cursor	✅
Cursor player video switching - active class on hover	✅
Timeline progress - updates width during playback	✅
Timeline animation - is-animated class	✅
Video ended - auto-advance with wrap (2→1)	✅
Click to switch video - updates all states	✅
Mobile arrows prev/next - wrap-around using currentlyPlaying	✅
Counter updates	✅
Link updates (main, mobile, cursor text)	✅
The key lesson: When build.min.js doesn't expose its modules globally, we replicate its behavior by:

Reading the minified code to understand the logic
Using the same patterns (e.g., currentlyPlaying variable instead of DOM queries)
Matching the exact class names and attributes it expects